name: flutter_maps
description: A Flutter app that mocks Uber.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  animate_do: ^1.7.5
  bloc: ^6.1.1
  cupertino_icons: ^1.0.0
  flutter_bloc: ^6.1.2
  geolocator: ^6.2.0
  google_maps_flutter: ^1.1.1
  permission_handler: ^5.0.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  very_good_analysis: ^1.0.4

flutter:
  uses-material-design: true
